<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="infrastructure.fx.controller.catalog.UbicacionesController">
    <top>
        <ToolBar>
            <Button text="Nuevo" onAction="#nuevo"/>
            <Button text="Guardar" onAction="#guardar"/>
            <Button text="Eliminar" onAction="#eliminar"/>
            <Button text="Refrescar" onAction="#refresh"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label text="Ubicaciones"/>
        </ToolBar>
    </top>
    <left>
        <VBox spacing="6">
            <padding>
                <Insets top="10" right="10" bottom="10" left="10"/>
            </padding>
            <Label text="Nombre"/>
            <TextField fx:id="txtNombre"/>
            <Label text="Ubicación padre (opcional)"/>
            <ComboBox fx:id="cmbPadre"/>

            <Label text="Nota: déjalo vacío para crear una ubicación principal." style="-fx-font-size:11; -fx-text-fill:#666;"/>

        </VBox>
    </left>
    <center>
        <TableView fx:id="tbl">
            <columns>
                <TableColumn fx:id="colId" text="ID" prefWidth="100"/>
                <TableColumn fx:id="colNombre" text="Nombre" prefWidth="250"/>
                <TableColumn fx:id="colPadre" text="Padre" prefWidth="220"/>
            </columns>
        </TableView>
    </center>
</BorderPane>
